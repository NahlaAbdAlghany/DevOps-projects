apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: ksops-config
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
# List all SOPS-encrypted YAMLs that you want ksops to handle
- secrets/wp-secret.yaml