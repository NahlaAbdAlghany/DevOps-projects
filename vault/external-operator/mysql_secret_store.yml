apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: db-secret-store
spec:
  provider:
    vault:
      server: "http://10.0.0.58:8200"
      path: "kv/secret"
      version: "v2"
      auth:
        tokenSecretRef:
          name: "vault-token"
          namespace: "mariadb"
          key: "token"
